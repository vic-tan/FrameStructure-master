<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.tanlifei.framestructure" >

    <!-- 手机移动网络 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 允许程序访问有关GSM网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".ui.GlobalApplication"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".testDemo.ui.activity.TestMainActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- 测试 -->
        <activity android:name=".testDemo.ui.activity.HtppTaskActivity"/>
        <activity android:name=".testDemo.ui.activity.ListViewActivity"/>
        <activity android:name=".testDemo.ui.activity.GridViewActivity"/>
        <activity android:name=".testDemo.ui.activity.DialogActivity"/>
        <activity android:name=".testDemo.ui.activity.LoadingDialogActivity"/>
    </application>

</manifest>
