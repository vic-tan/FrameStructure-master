<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/layout_root"
    style="@style/common_match_parent"
    android:background="@color/common_default_bg"
    android:gravity="center"
    android:orientation="vertical"
    android:visibility="gone">


    <include layout="@layout/common_loading_progress_bar"/>

    <ImageView
        android:id="@+id/iv_logo"
        style="@style/common_wrap_content"
        android:src="@mipmap/common_prompt_error_icon"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_hint"
        style="@style/common_match_parent_with"
        android:layout_marginTop="15px"
        android:gravity="center"
        android:textSize="@dimen/common_size_three_level" />

</LinearLayout>